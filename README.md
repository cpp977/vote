# Vote

[![CI](https://github.com/cpp977/vote/actions/workflows/dart.yml/badge.svg)](https://github.com/cpp977/vote/actions/workflows/dart.yml)

<p align="center">
  <img alt="Vote logo" src="https://raw.githubusercontent.com/cpp977/vote/main/assets/icon/app_icon.png" width="160" />
</p>

Cross-platform Flutter frontend for voting-style questionnaires and results.

## Quick summary
Vote is a Flutter-based UI for presenting questions and collecting votes. It includes:
- A minimal routing surface (login, home, account-locked, etc.) defined in `lib/main.dart`.
- Localization support with ARB files in `lib/l10n` and `l10n.yaml` configured.
- A simple folder structure separating controllers, pages, services, models and widgets.

## Stack
- Language(s): Dart (Flutter)
- Framework / runtime: Flutter (stable)
- Notable packages: see `pubspec.yaml` for all dependencies (app uses standard Flutter packages and a small set of platform libraries)

## Project layout (top-level)
```
.gitignore
.github/                     GitHub workflows (CI)
AGENTS.md
README.md
pubspec.yaml                 Dart/Flutter manifest
pubspec.lock
l10n.yaml                    Flutter localization config
assets/
  icon/app_icon.png          App logo used in the repo
lib/
  main.dart                  App entry + routing (title: "Vote")
  config/
  controllers/
  models/
  pages/
  services/
  utils/
  widgets/
test/                        Tests (mirror of lib structure)
android/, linux/, web/       Platform folders generated by Flutter
```

How it fits together:
- `lib/main.dart` builds `MyApp` with a MaterialApp and route table (e.g. `/login`, `/account-locked`, `/`).
- Pages live under `lib/pages`, business logic in `lib/controllers` and `lib/services`, UI pieces in `lib/widgets`.
- Localization files are in `lib/l10n` and configured by `l10n.yaml` (template `app_en.arb` → generates `AppLocalizations`).

## Getting started (developer)
From a fresh clone, install Flutter and run the commands below. This repo uses the standard Flutter/Dart toolchain.

Commands
- Build (release/standalone):
  - /opt/flutter/bin/dart compile exe lib/main.dart -o bin/vote
  - or use `flutter build` for platform builds
- Lint:
  - /opt/flutter/bin/dart analyze
  - or `flutter analyze`
- Run all tests:
  - /opt/flutter/bin/dart test
- Run a single test:
  - /opt/flutter/bin/dart test path/to/file_test.dart -n "test name"
- Watch tests:
  - /opt/flutter/bin/dart test --watch
- Format:
  - /opt/flutter/bin/dart format .

Environment / quick run (dev):
1. Ensure Flutter SDK installed and `flutter`/`dart` on PATH (or use direct path above).
2. From project root:
   - flutter pub get
   - flutter run

If you use the standalone dart compile path shown above, adjust for your environment.

## Localization
- `l10n.yaml` is present and points ARB templates to `lib/l10n` and will generate `AppLocalizations`.
- English is configured as the template/source locale.

## CI / Test results
- A GitHub Actions workflow is configured at `.github/workflows/dart.yml`.
- The README includes a workflow badge that reflects the current status:
  - Markdown badge (renders live):  
    `[![CI](https://github.com/cpp977/vote/actions/workflows/dart.yml/badge.svg)](https://github.com/cpp977/vote/actions/workflows/dart.yml)`
- Click the badge to view workflow run logs and artifacts in GitHub Actions.

## Logo
- The app icon is included at `assets/icon/app_icon.png`.
- In README it is referenced from the repository raw URL:
  - `https://raw.githubusercontent.com/cpp977/vote/main/assets/icon/app_icon.png`

## Tests & Quality
- Tests live in `test/` matching `lib/` structure. Run tests with the commands above.
- Use the `analysis_options.yaml` and `dart format .` before committing to keep code style consistent.

## Contributing
- Follow the project's code style:
  - Alphabetical imports, separate groups by blank line
  - Explicit types, use `Future<T>` for async
  - Document public members with Dartdoc `///`
  - Run `/opt/flutter/bin/dart format .` before creating PRs
- Open PRs against `main` and include a descriptive title and short body.

## Notes / TODOs (observations from a quick scan)
- `lib/main.dart` shows the main routes and sets `title: 'Vote'` — verify any branding or wording you want here.
- Localization is set up; add localized ARB files (other than `app_en.arb`) to support more languages.
- CI workflow `dart.yml` exists; consider adding test coverage upload or badges for coverage if desired.

---
If you'd like, I can:
- Create this README.md in the repository (commit or PR).
- Add/update a CI badge to point to a specific workflow run or add coverage badges.
- Open a small PR that replaces the existing template README with this content.
